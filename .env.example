# Exemplo de arquivo .env para o Assessor AI Crawler
# Copie este arquivo para .env e preencha com seus valores

# API Keys (se necessário)
# WEAVIATE_URL=http://localhost:8080
# WEAVIATE_API_KEY=your_api_key_here
# WEAVIATE_CLASS=Bill
# GEMINI_API_KEY=your_gemini_api_key_here
# OPENAI_APIKEY=your_openai_api_key_here

# Configurações do Scrapyd (Docker)
SCRAPYD_BIND_ADDRESS=0.0.0.0
SCRAPYD_HTTP_PORT=6800

# Configurações do ScrapydWeb (Docker)
SCRAPYDWEB_BIND=0.0.0.0:5000
# ScrapydWeb acessa Scrapyd através do proxy Nginx (dentro da rede Docker)
SCRAPYD_SERVERS=nginx:8080/scrapyd

# Configurações do LogParser (Docker)
SCRAPYD_LOGS_DIR=/app/logs
